type Query {
    _: String
}

type Mutation {
    _: String
}

type Subscription {
    _: String
}

interface Node {
    id: ID!
}

type PageInfo {
    hasPreviousPage: Boolean!
    hasNextPage: Boolean!
    startCursor: String
    endCursor: String
}
